using UnityEngine;

public class InputManager : RyoMonoBehaviour
{
/*    private static InputManager instance;
    public static InputManager Instance => instance;

    [Header("Input")]

    [SerializeField] private bool _bPressedMove;
    [SerializeField] private bool _bPressingMove;
    public bool BPressedMove => _bPressedMove;
    public bool BPressingMove => _bPressingMove;

    protected override void Awake()
    {
        if (InputManager.instance != null) return;

        InputManager.instance = this;
        base.Awake();

    }

    private void Update()
    {
        this._bPressedMove = Input.GetKeyDown(KeyCode.A) || Input.GetKeyDown(KeyCode.D)
                        || Input.GetKeyDown(KeyCode.LeftArrow) || Input.GetKeyDown(KeyCode.RightArrow);

        this._bPressingMove = Input.GetKey(KeyCode.A) || Input.GetKey(KeyCode.D)
                        || Input.GetKey(KeyCode.LeftArrow) || Input.GetKey(KeyCode.RightArrow);
    }*/

}
